#!/usr/bin/env ruby

require 'rirera'

begin
  while true do
    system "clear"
    rrr = Rirera.new(ARGV[0])
    rrr.print_result
    puts ""
    puts "Again? (y/n)"
    exit if $stdin.gets.chomp == "n"
  end
rescue Interrupt => e
    puts ""
    puts "Exit..."
end
